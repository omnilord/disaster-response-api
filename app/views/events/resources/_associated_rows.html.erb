<% if list.blank? %>
  <tr>
    <td colspan="3" class="text-center associated-none"><%= t(:none) %></td>
  </tr>
<% else %>
  <% list.each do |r| %>
    <%= render 'events/resources/associated_row', event: event, item: r %>
  <% end %>
<% end %>
