<%= render 'pages/page' %>

<%= link_to 'Add New Page', new_page_path, class: 'button button-outline' %>

<% @pages.each do |page| %>
  <hr/>
  <h4><%= link_to "#{page.title} (#{page.page})", edit_page_path(page) %></h4>
<% end %>
